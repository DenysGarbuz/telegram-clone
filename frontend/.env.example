# Copy this file to `.env.local` (preferred) or `.env` and fill in real values.
# Next.js requires the `NEXT_PUBLIC_` prefix for any var that must be readable
# in the browser.

# Base URL of the backend API + Socket.IO server (no trailing slash).
NEXT_PUBLIC_SERVER_URL=http://localhost:3003
